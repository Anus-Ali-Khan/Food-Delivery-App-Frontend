type ColorTypes = {
  [key: string]: string;
};

type fontTypes = {
  [key: string]: string;
};

export const colors: ColorTypes = {
  PRIMARY: '#FFB803',
  SECONDARY: '#721A16',
};

export const fonts: fontTypes = {
  PRIMARY: 'Urbanist-VariableFont_wght',
};
